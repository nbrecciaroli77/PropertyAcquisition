# Test credentials

Milestone 1 has no authentication and no accounts. The authenticated shell is reachable directly at `/app/today` via the "Preview the shell with synthetic data" link on `/`.

Accounts will be created in Milestone 2.
